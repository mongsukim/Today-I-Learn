# 2025-11-03~ 11.-7 업무 일지

## 통근버스 폴리곤 FE

### 1. 기능 개선 및 버그 수정

#### 경로 편집 기능 안정화
- 미세경로 수정 완료 시 자동으로 편집모드 종료되도록 개선
- 정류소 변경 및 미세경로 업데이트 시에도 편집모드가 종료되며, 사용자가 보던 지도 줌/위치 상태 유지되도록 수정
- 편집모드 중 정류장명이 표시되지 않도록 처리
- 꼭지점 삭제 시 alert 없이 바로 삭제 가능하도록 변경

#### 렌더링 및 빌드 오류 해결
- 무한 렌더링 발생 원인 제거
- 빌드 에러 수정 및 timeout을 60초로 재설정

### 2. UI / 스타일 작업
- 노선명 가로 스크롤 처리 (길이가 긴 경우 대응)
- 전체 CSS 구조 정리 및 불필요 스타일 정돈

### 3. 코드 리팩토링
- /home으로 가는 링크 제거, 사업장별 주소 생성
- StationMarker 컴포넌트의 다수 props를 그룹화하여 구조 단순화
- usePolylineEdit.ts 파일 내 여러 함수를 분리하여 가독성 향상

---

## 샌디 BackOffice FE

### 쿠팡 풀필먼트 총무성업무
- 게시글 수정 페이지 퍼블리싱 및 API 연결
- 글쓰기 페이지 퍼블리싱 및 API 연결

### 쿠팡 풀필먼트 구매대행업무
- 게시글 수정 페이지 퍼블리싱 및 API 연결
- 글쓰기 페이지 퍼블리싱 및 API 연결

---

## 샌디앱 FE

### 비밀번호 찾기 기능 외부 접근 개선
- BO/Admin 페이지에서 샌디앱 비밀번호 찾기 페이지로 직접 접근 가능하도록 구현
- `from=bo` 쿼리 파라미터를 통한 외부 접근 허용 로직 추가
  - App.tsx: 초기 로그인 체크에서 `from=bo` 파라미터 확인 로직 추가
  - AuthGuard: 라우트 이동 시 `from=bo` 파라미터 체크 추가
  - FindPassword: 외부 접근 시 뒤로가기 버튼 숨김 처리
  - ResetPassword: 비밀번호 재설정 완료 후 외부 접근인 경우 창 닫기 처리
- BO 프로젝트 Login 페이지에 비밀번호 찾기 링크 추가 (`?from=bo` 파라미터 포함)
